# Claude Memory Kit (CMK)

# Required for synthesis (reflect, auto_extract, identity generation)
ANTHROPIC_API_KEY=<your-api-key>

# Model (default: claude-opus-4-6)
# ANTHROPIC_MODEL=claude-opus-4-6

# Local storage path (default: ~/.claude-memory)
# MEMORY_STORE_PATH=~/.claude-memory

# ---- Cloud mode (optional) ----
# Set QDRANT_URL to switch from embedded local to Qdrant Cloud.
# When unset, vectors are stored locally (no server needed).
# QDRANT_URL=https://your-cluster.cloud.qdrant.io:6334
# QDRANT_API_KEY=<your-qdrant-api-key>

# ---- Database (optional) ----
# Set DATABASE_URL to use Supabase Postgres instead of local SQLite.
# When unset, auth data is stored in local SQLite (no external DB needed).
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ---- Auth (optional, requires DATABASE_URL) ----
# Set BetterAuth keys to enable multi-tenant auth.
# When unset, all data is scoped to user_id="local" (no login needed).
# BETTER_AUTH_URL=https://your-dashboard.example.com
# BETTER_AUTH_SECRET=<your-random-secret-32-chars>

# ---- Social login (optional, requires auth) ----
# GITHUB_CLIENT_ID=<your-github-client-id>
# GITHUB_CLIENT_SECRET=<your-github-client-secret>
# GOOGLE_CLIENT_ID=<your-google-client-id>
# GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# ---- CORS (optional) ----
# Comma-separated list of allowed origins for the API.
# Defaults to http://localhost:5555,http://localhost:3000 when unset.
# CORS_ORIGINS=https://your-dashboard.example.com,https://your-app.example.com

# ---- Flow Mode (experimental) ----
# Compress tool output in real time to extend session length.
# CMK_FLOW_MODE=true
# CMK_FLOW_THRESHOLD=2000
# CMK_FLOW_SKIP_TOOLS=mcp__my-server__my_tool

# Dashboard keys (set in dashboard/.env.local)
# NEXT_PUBLIC_AUTH_ENABLED=true
# NEXT_PUBLIC_BETTER_AUTH_URL=https://your-dashboard.example.com
